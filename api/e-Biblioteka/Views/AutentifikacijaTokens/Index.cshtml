@model IEnumerable<e_Biblioteka.ModulAutentifikacija.Models.AutentifikacijaToken>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vrijednost)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.korisnickiNalog)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VrijemeEvidentiranja)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IPadresa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Vrijednost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.korisnickiNalog.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VrijemeEvidentiranja)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IPadresa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
